apiVersion: ocular.crashoverride.run/v1beta1
kind: ClusterDownloader
metadata:
  name: pypi
spec:
  container:
    command:
    - /entrypoint
    image: downloaders
    imagePullPolicy: IfNotPresent
    name: pypi
    resources: {}
